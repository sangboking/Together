import React from "react";
import styled from "styled-components";

export const MainSection = () => {
  return <Section>MainSection</Section>;
};

const Section = styled.section`
  height: 100vh;
  scroll-snap-align: start;
  background-color: yellow;
`;
